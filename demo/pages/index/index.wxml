<!--pages/progress/progress.wxml-->
<view class="grid">
  <view wx:for="{{blocks}}" wx:key="index" class="block">
    <view class="outer_circle"></view>
    <view class="inner_circle"></view>
    <canvas canvas-id="{{item.name}}" class="ring"></canvas>
  </view>
</view>
<button  type="primary" bindtap="next_page">下一页</button>

<!-- <view class="grid">
  <view class="block" bindtap="add_img">
    <image src="{{imgPath[0]}}" mode="aspectFit"></image>
  </view>
</view> -->

